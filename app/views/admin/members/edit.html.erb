<h1 class="center">Editar socio</h1>

<div class="container-form">
  <%= form_for @member, url: admin_member_path(@member), method: :put do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>

<%= link_to 'Volver', admin_members_path %>