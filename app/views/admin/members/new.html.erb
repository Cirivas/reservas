<h2 class="center">Nuevo socio</h2>
<div class="container-form">
  <%= form_for @member, url: admin_members_path do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>

<%= link_to 'Volver', admin_members_path %>